<%= form_with model: [ @hotel, @hotel.reviews.build ] do |form| %>
  <p>
    <%= form.label :rating %><br>
    <%= form.text_area :rating %>
  </p>
  <p>
    <%= form.label :description %><br>
    <%= form.text_area :description %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>